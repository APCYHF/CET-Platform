[build]
builder = "NIXPACKS"
buildCommand = "mvn clean package -DskipTests"

[deploy]
startCommand = "java -jar target/cet-prep-platform-1.0.0.jar --spring.profiles.active=prod"

[service]
name = "cet-platform-backend"
